apiVersion: v1
kind: ConfigMap

metadata:
  name: rapido-config
  namespace: rapido-prod

data:
  DB_HOST: postgres
  DB_PORT: "5432"
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  RABBITMQ_HOST: rabbitmq
  RABBITMQ_PORT: "5672"