apiVersion: batch/v1

kind: Job

metadata:
  name: migration-job

spec:

  template:

    spec:

      containers:

        - name: migration

          image: migration-service:latest

      restartPolicy: Never

  backoffLimit: 3