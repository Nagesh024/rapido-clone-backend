version: '3.9'

services:

  postgres:
    image: postgres:16

    environment:
      POSTGRES_DB: platform_engineering
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Nagesh2425

    ports:
      - "5432:5432"

  platform-engineering:

    build: .

    ports:
      - "8088:8088"

    depends_on:
      - postgres