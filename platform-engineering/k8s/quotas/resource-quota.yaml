apiVersion: v1
kind: ResourceQuota

metadata:
  name: platform-resource-quota

spec:

  hard:

    requests.cpu: "4"
    requests.memory: 8Gi

    limits.cpu: "8"
    limits.memory: 16Gi

    persistentvolumeclaims: "10"

    requests.storage: 100Gi

    pods: "50"

    services: "20"