server:
  port: 8090

spring:
  application:
    name: auth-server

  datasource:
    url: jdbc:postgresql://localhost:5432/rapido_security_db
    username: postgres
    password: Nagesh2425
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics

logging:
  level:
    org.springframework.security: DEBUG