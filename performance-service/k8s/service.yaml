apiVersion: v1
kind: Service

metadata:
  name: performance-service
  namespace: rapido-performance

spec:

  selector:
    app: performance-service

  ports:

  - protocol: TCP
    port: 80
    targetPort: 8080

  type: LoadBalancer