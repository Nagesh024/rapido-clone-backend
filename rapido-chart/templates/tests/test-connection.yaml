apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rapido-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rapido-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rapido-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
